@model Pedido

@{
    ViewData["Title"] = "Detalle de pedido";
}

<h1 class="h3 mb-3"><i class="bi bi-receipt me-2"></i>Pedido #@Model.Id</h1>

@if (TempData["Success"] is not null)
{
    <div class="alert alert-success"><i class="bi bi-check-circle me-1"></i>@TempData["Success"]</div>
}

<div class="card mb-4">
    <div class="card-body p-4">
        <dl class="row mb-0">
            <dt class="col-sm-2"><i class="bi bi-person me-1"></i>Cliente</dt>
            <dd class="col-sm-10">@Model.Cliente?.Nombre (@Model.Cliente?.Cedula)</dd>
            <dt class="col-sm-2"><i class="bi bi-person-badge me-1"></i>Usuario</dt>
            <dd class="col-sm-10">@Model.Usuario?.Email</dd>
            <dt class="col-sm-2"><i class="bi bi-calendar-event me-1"></i>Fecha</dt>
            <dd class="col-sm-10">@Model.Fecha.ToString("dd/MM/yyyy HH:mm")</dd>
            <dt class="col-sm-2">Estado</dt>
            <dd class="col-sm-10">
                <span class="badge bg-success-subtle text-success-emphasis"><i class="bi bi-check-circle me-1"></i>@Model.Estado</span>
            </dd>
        </dl>
    </div>
</div>

<div class="table-responsive mb-4">
    <table class="table table-hover align-middle mb-0">
        <thead>
            <tr>
                <th>Producto</th>
                <th>Cantidad</th>
                <th>Precio unit.</th>
                <th>Descuento</th>
                <th>Impuesto</th>
                <th>Total línea</th>
            </tr>
        </thead>
        <tbody>
@foreach (var detalle in Model.Detalles)
{
            <tr>
                <td><i class="bi bi-cookie text-body-secondary me-1"></i>@detalle.Producto?.Nombre</td>
                <td>@detalle.Cantidad</td>
                <td>@detalle.PrecioUnit.ToString("C")</td>
                <td>@detalle.Descuento%</td>
                <td>@detalle.ImpuestoPorc%</td>
                <td class="fw-semibold">@detalle.TotalLinea.ToString("C")</td>
            </tr>
}
        </tbody>
    </table>
</div>

<div class="row justify-content-end">
    <div class="col-md-4">
        <div class="card">
            <div class="card-body">
                <div class="d-flex justify-content-between">
                    <span>Subtotal</span>
                    <span>@Model.Subtotal.ToString("C")</span>
                </div>
                <div class="d-flex justify-content-between">
                    <span>Impuestos</span>
                    <span>@Model.Impuestos.ToString("C")</span>
                </div>
                <hr />
                <div class="d-flex justify-content-between fw-bold fs-5">
                    <span>Total</span>
                    <span>@Model.Total.ToString("C")</span>
                </div>
            </div>
        </div>
    </div>
</div>

<a asp-action="Index" class="btn btn-outline-secondary mt-3"><i class="bi bi-arrow-left me-1"></i>Volver</a>
